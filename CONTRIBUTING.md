# 기여 가이드

브랜치는 `feat/기능`, `fix/버그`, `refactor/리팩터링`, `chore/작업`처럼 목적을 드러내 주세요. 커밋은 `feat:`, `fix:`, `refactor:`, `chore:` 접두사를 사용하고 한 커밋에 한 의도를 담습니다.

작업 → `uv run pytest`/`uv run ruff check .` 확인 → PR 생성 → 담당자 리뷰 → `main` 병합 순서입니다. 모듈 경계를 넘는 변경은 관련 담당자를 리뷰어로 지정합니다.

