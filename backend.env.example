# 형식만 보여주는 예시다. 실제 값은 깃에 절대 넣지 않는다.
# 진짜 값은 서버의 ~/deploy/backend.env 에만 있고, 그 파일은 chmod 600 이다.

SPRING_PROFILES_ACTIVE=prod

DB_HOST=<RDS 엔드포인트>
DB_PORT=3306
DB_NAME=<DB 이름>
DB_USERNAME=<계정>
DB_PASSWORD=<비밀번호 관리자 참조>
