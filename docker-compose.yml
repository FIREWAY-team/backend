services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_DATABASE: firedispatch
      MYSQL_ROOT_PASSWORD: root
    ports: ["3306:3306"]
    volumes: ["fireway-mysql:/var/lib/mysql"]
volumes:
  fireway-mysql:
